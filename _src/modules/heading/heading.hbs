{{!--
* background for background-color, auto set padded if set
* color for text color
--}}
<div class="heading-page{{#if background}} padded {{/if}}"{{#if background}} style="background-color:{{background}}; color: {{color}};{{/if}}">
  <h1>Page title or article title</h1>
  <p>Lorem ipsum dolor sit amet, id has quem dicam. Eam at stet verear expetendis.</p>
</div>
